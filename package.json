{
  "name": "react-reusable",
  "version": "1.0.1",
  "main": "index.js",
  "scripts": {
    "publish": "for d in ./*/ ; do (cd \"$d\" && npm run build); done;",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "lerna": "^2.0.0-rc.5"
  }
}
