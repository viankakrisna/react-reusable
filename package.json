{
  "name": "react-reusable",
  "version": "1.0.5",
  "main": "index.js",
  "scripts": {
    "prepublish": "for d in ./*/ ; do (cd \"$d\" && npm run build); done;",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "lerna": "^2.0.0-rc.5"
  }
}
